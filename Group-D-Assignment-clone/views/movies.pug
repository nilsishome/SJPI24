extends template/main.pug

block headStuff
    title Filmer

block scripts
  script(type='module', src='/static/js/moviesPage.js' defer)

block content
    h1(class='movies__header') Våra filmer
    ul(class='movies__list')
        each movie in movies
          div(class='movies__list_containers')
            a(href='/movies/' + movie.id)
              - if (movie.image)
                img(src= movie.image.url)
              li(class='movies__list_item')
                p #{movie.title}
            