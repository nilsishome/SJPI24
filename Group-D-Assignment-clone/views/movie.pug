extends template/main.pug

block headStuff
    title Film

block content
    a(class='link_to_movies', href='/movies') Tillbaka till filmer
    article(class='movie')
        h1(class='movie__header') #{movie.title}
        div(class='movie_container')
            div(class='movie_container__description') #{movie.intro}
            - if (movie.image)
            img(src= movie.image.url)