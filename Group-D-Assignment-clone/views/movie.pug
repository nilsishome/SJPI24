extends template/main.pug

block headStuff
    title Film

block content
    a(href='/movies') Tillbaka till filmer
    article
        h1 #{movie.title}
        div #{movie.intro}
        - if (movie.image)
          img(src= movie.image.url)